% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{merge.netsim.bip}
\alias{merge.netsim.bip}
\title{Merge Model Simulations Across netsim.bip Objects}
\usage{
\method{merge}{netsim}(x, y, keep.transmat = TRUE, keep.network = TRUE,
  keep.nwstats = TRUE, keep.other = TRUE, param.error = TRUE, ...)
}
\arguments{
\item{x}{An \code{EpiModel} object of class \code{\link{netsim.bip}}.}

\item{y}{Another \code{EpiModel} object of class \code{\link{netsim.bip}},
with the identical model parameterization as \code{x}.}

\item{keep.transmat}{If \code{TRUE}, keep the transmission matrices from the
original \code{x} and \code{y} elements.}

\item{keep.network}{If \code{TRUE}, keep the \code{networkDynamic.bip} objects
from the original \code{x} and \code{y} elements.}

\item{keep.nwstats}{If \code{TRUE}, keep the network statistics (as set by
the \code{nwstats.formula} parameter in \code{control.netsim.bip}) from
the original \code{x} and \code{y} elements.}

\item{keep.other}{If \code{TRUE}, keep the other simulation elements (as set
by the \code{save.other} parameter in \code{control.netsim.bip}) from the
original \code{x} and \code{y} elements.}

\item{param.error}{If \code{TRUE}, if \code{x} and \code{y} have different
params (in \code{param.net.bip}) or controls (passed in \code{control.net.bip})
an error will prevent the merge. Use \code{FALSE} to override that
check.}

\item{...}{Additional merge arguments (not currently used).}
}
\description{
Merges epidemiological data from two independent simulations of
             stochastic bipartite network models from \code{netsim.bip}.
}
\details{
This merge function combines the results of two independent simulations of
\code{\link{netsim.bip}} class models, simulated under separate function calls. The
model parameterization between the two calls must be exactly the same, except
for the number of simulations in each call. This allows for manual
parallelization of model simulations.

This merge function does not work the same as the default merge, which allows
for a combined object where the structure differs between the input elements.
Instead, the function checks that objects are identical in model parameterization
in every respect (except number of simulations) and binds the results.
}
\examples{
# Network model
nw <- network.initialize.bip(n = 100, directed = FALSE)
coef.diss <- dissolution_coefs.bip(dissolution = ~offset(edges), duration = 10)
est <- netest(nw, formation = ~edges, target.stats = 25,
              coef.diss = coef.diss, verbose = FALSE)

# Epidemic models
param <- param.net.bip(inf.prob = 1)
init <- init.net.bip(i.num = 1)
control <- control.net.bip(type = "SI", nsteps = 20, nsims = 2,
                       save.nwstats = TRUE,
                       nwstats.formula = ~edges + degree(0),
                       verbose = FALSE)
x <- netsim.bip(est, param, init, control)
y <- netsim.bipest, param, init, control)

# Merging
z <- merge(x, y)
x$epi
y$epi
z$epi

}
\keyword{extract}
